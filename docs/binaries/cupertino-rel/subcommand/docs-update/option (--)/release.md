# --release

Continue with tag and upload after bump.

## Synopsis

```bash
cupertino-rel docs-update --release
```

## Description

After bumping the version, continue with the full release workflow (tag, push, upload databases).
